import { NgxTippyDirective } from './ngx-tippy.directive';

describe('NgxTippyDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxTippyDirective();
    expect(directive).toBeTruthy();
  });
});
